import 'package:flutter/material.dart';

class UpdateNewsScreen extends StatelessWidget {
  const UpdateNewsScreen({super.key, required this.id});

  final int id;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
